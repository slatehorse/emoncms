<!-- run tests with `docker run -v $(pwd):/app --rm phpunit/phpunit -c phpunit.xml` -->
<phpunit bootstrap="tests/bootstrap.php">
  <testsuites>
    <testsuite name="emoncms-core">
      <directory>tests</directory>
    </testsuite>
  </testsuites>
</phpunit>